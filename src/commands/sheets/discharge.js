const { EmbedBuilder } = require("discord.js");

module.exports = {
  name: 'discharge',
  description: 'Discharge a user (CC+)',
  aliases: [],
  callback: async (client, message, gsapi, ssid) => {
    const embed = new EmbedBuilder();

  }
}